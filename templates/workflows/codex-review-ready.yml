name: Codex Review Ready

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed
  pull_request_review_comment:
    types:
      - created
      - edited
      - deleted
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: read
  statuses: write

jobs:
  codex-review-ready:
    name: codex-review-ready-reconcile
    runs-on: ubuntu-24.04
    timeout-minutes: 35

    steps:
      - name: Reconcile Codex Review ready status
        if: github.event_name != 'schedule' && github.event_name != 'workflow_dispatch'
        # Pin to an exact commit SHA in mature repos when practical.
        uses: heurema/repo-governance/actions/codex-review-ready@v0.5.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          review-author-logins: chatgpt-codex-connector
          ignore-outdated: 'true'
          status-context: codex-review-ready
          poll-seconds: '1800'
          poll-interval-seconds: '10'
          timeout-state: failure
          reaction-grace-seconds: '480'

      - name: Reconcile open PRs on schedule or dispatch
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        # Pin to an exact commit SHA in mature repos when practical.
        uses: heurema/repo-governance/actions/codex-review-ready@v0.5.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          review-author-logins: chatgpt-codex-connector
          ignore-outdated: 'true'
          status-context: codex-review-ready
          poll-seconds: '0'
          poll-interval-seconds: '10'
          timeout-state: pending
          reaction-grace-seconds: '480'
          max-open-prs: '50'

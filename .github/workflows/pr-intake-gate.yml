name: PR Intake Gate

on:
  pull_request_target:
    types:
      - opened
      - edited
      - reopened
      - synchronize
      - labeled
      - unlabeled
      - ready_for_review

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  pr-intake-gate:
    name: pr-intake-gate
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout trusted base code
        # Pinned from actions/checkout@v4. Update only intentionally.
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          persist-credentials: false

      - name: Run PR intake gate
        uses: heurema/repo-governance/actions/pr-intake-gate@58c1878e1e16c4139e35594aab6d1cb00f1c1018
        with:
          policy-path: .github/pr-intake-gate.yml
          github-token: ${{ secrets.GITHUB_TOKEN }}
          codex-review-require-current-review: 'false'
          codex-review-wait-seconds: '0'
          codex-review-resolution-wait-seconds: '480'
          codex-review-poll-interval-seconds: '10'
